import { FirstZeroNumPipe } from './first-zero-num.pipe';

describe('FirstZeroNumPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstZeroNumPipe();
    expect(pipe).toBeTruthy();
  });
});
